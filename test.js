// let dao = require('./database/dao')

// const login = dao.findByUserName('luan')
//     .then(([rows]) => {
//       if(rows.length == 0) return done(null, false)

//       let user = rows[0]
//       console.log(user)

//       for (const key in user) {
//         console.log(`${key}: ${user[key]}`)
//       }

//     }).catch(error => {
//       console.log(error)
//       return done(error, null)
//     })

// console.log(login)